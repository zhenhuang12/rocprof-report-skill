# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.pytest_cache/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Compiled harness artifacts (the template compiles to these; don't commit)
*.o
*.d
*.so
# Don't ignore source .hip / .cpp / .cu files — those are part of the skill

# rocprofv3 / rocprof-compute / ATT scratch — users generate their own
profile/
*.rpd
*.db-journal
